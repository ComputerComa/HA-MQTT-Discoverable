# API Reference

This section is generated directly from the XML doc comments in the [HaMqttDiscoverable source](https://github.com/ComputerComa/HA-MQTT-Discoverable/tree/main/src/HaMqttDiscoverable). It's rebuilt on every push to `main`, so it always matches the latest code.

A good place to start:

- <xref:HaMqttDiscoverable.HaMqttConnection> - the MQTT connection every entity is created with.
- <xref:HaMqttDiscoverable.Device> - groups entities under one device in Home Assistant.
- <xref:HaMqttDiscoverable.Entities.HaEntity`1> - the base class every entity type derives from.

Then browse the `HaMqttDiscoverable.Entities` namespace in the sidebar for the concrete entity types (`Sensor`, `Switch`, `Light`, `MediaPlayer`, ...) and their `*Config` classes.
